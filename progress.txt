Date: 2026-08-28
Task: Implement GitHub issue #715: "refactor(build): extract buildSerializer module with full test coverage"
Summary: Extracted all serialization and validation business logic from the `useBuildFileManager` hook into a new standalone, plain-object module `src/utils/build/buildSerializer.ts`. Exposes `saveBuild` and `loadBuild` functions. Written 100% test coverage in `src/utils/build/buildSerializer.test.ts` without relying on DOM emulation or `renderHook`.
Key Learnings:
- Vitest's `vi.mock` module hoisting executes before mock variable declarations unless the variables are hoisted. Utilizing `vi.hoisted` provides a clean, native way to declare and resolve store state mocks without temporal dead zone ReferenceErrors.
- Plain business logic functions decoupled from React hooks can be thoroughly and cleanly unit-tested as plain async function calls with minimal mocks, making the test suite faster and easier to maintain.

Date: 2026-08-28
Task: Implement GitHub issue #716: "refactor(build): thin useBuildFileManager to a DOM adapter"
Summary: Simplified the `useBuildFileManager` hook into a thin DOM adapter that delegates all serialization, validation, and state restoration logic to the `buildSerializer` module. The hook is now solely responsible for retrieving valid ship types via `useFetchShipTypesSuspense` and managing DOM download and loading integrations.
Key Learnings:
- Restricting React hooks to DOM/browser interactions and delegating all core validation and business logic to plain utility modules leads to cleaner, more decoupled components.
- In Vitest/Testing Library tests, when mocking global DOM methods (e.g., `document.body.appendChild`), care must be taken to only spy on the calls or avoid blocking default behavior that the React runtime requires to render hooks. Using `vi.spyOn` on specific properties or prototypes is a safer approach than stubbing global methods completely.

Date: 2026-08-28
Task: Implement GitHub issue #718: "refactor(build): decouple buildSerializer from React and Zustand stores"
Summary: Refactored `buildSerializer.ts` to be completely decoupled from React and Zustand store singletons. `loadBuild` now accepts raw JSON text and valid ship types, returning the parsed/validated state object to the caller. `saveBuild` accepts state parameters, computes the integrity checksum, and formats the output into a JSON structure with alphabetically sorted top-level keys while preserving the original insertion order of nested state keys. Updated `useBuildFileManager.ts` and the test suites.
Key Learnings:
- Separation of Concerns: Decoupling core serialization from React transitions and global Zustand stores converts the module into a pure utility, removing store mocking needs in the unit tests.
- JSON Serialization Order: Constructing output structures with alphabetically sorted top-level keys while preserving nested key insertion order satisfies both linter requirements and backward-compatible file integrity checksums.

Date: 2026-08-28
Task: Implement GitHub issue #719: "refactor(build): thin useBuildFileManager to a DOM adapter with pending state"
Summary: Refactored `useBuildFileManager.ts` to utilize React 19's `useTransition` hook, allowing it to expose a reactive `isPending` state during asynchronous file operations. Wrapped the file reading, loading, and store state restoration inside the transition callback. Thinned `useLoadBuild.tsx` by removing its local pending state and consuming the hook's `isPending` state directly. Added comprehensive unit tests for `isPending` state changes.
Key Learnings:
- React 19 Async Transitions: React 19's `useTransition` allows tracking pending states of async functions natively. Wrapping async file operations and store updates inside the transition callback simplifies UI pending state management.
- Mocking useTransition in Vitest: Custom mocks of `useTransition` that leverage React's actual state hooks (like `actual.useState`) enable reliable, reactive test assertions of pending states for asynchronous hooks.

Date: 2026-08-28
Task: Implement GitHub issue #720: "test(build): rewrite useBuildFileManager tests with loading states and download triggers"
Summary: Rewrote the complete test suite for `useBuildFileManager` hook. Verified that `isPending` starts `false`, transitions to `true` while the file is processed/serialized or read/parsed, and returns to `false` when complete or when an error is thrown. Verified the complete download simulation sequence (URL creation, anchor element instantiation, append/removal, click, and URL revocation). Added error-handling and state-cleanup tests.
Key Learnings:
- Testing DOM Side-Effects after renderHook: Spying on DOM methods like `document.body.appendChild` should happen *after* calling `renderHook` to prevent tracking mounting actions executed by the test renderer.
- Comprehensive State Cleanup Assertions: Ensuring `isPending` resets to `false` and any temporary browser resources are cleaned up upon failures guards against UI lockouts and memory leaks in real applications.

Date: 2026-08-28
Task: Implement GitHub issue #724: "refactor(build): correct React transition boundaries in useBuildFileManager"
Summary: Refactored the `useBuildFileManager` hook to run asynchronous file I/O, validation, and DOM download triggers outside of React transitions. Replaced `useTransition()` progress tracking with a standard `useState(false)` pending flag inside the hook. Implemented a new `restoreTechState` action on the Zustand technology store (`useTechStore`) to encapsulate technology state restoration and legacy fallback merging, and consumed it in the hook. Wrapped only store update calls (`restoreGridState`, `restoreTechState`, and `setSelectedPlatform`) in `startTransition`. Updated `useBuildFileManager.test.ts` and `techStore.test.ts` to assert that pending state transitions and DOM download side-effects behave correctly under the new model.
Key Learnings:
- Asynchronous operations, network/disk I/O, and DOM download triggers must not run inside React transitions. Wrapping them in transitions is a performance anti-pattern. React transitions should wrap only synchronous state updates.
- Encapsulating complex multi-field state restorations into single store actions (like `restoreTechState`) maintains clean modular store boundaries and decouples custom hooks from state schemas.
- In Vitest hook rendering tests, any state changes triggered by asynchronous helper calls (such as validation errors) must be wrapped in React's `act(...)` utility to prevent development warning logs.

Date: 2026-08-28
Task: Implement GitHub issue #723: "refactor(build): re-type grid state parameters and extract checksum builder"
Summary: Re-typed `SaveBuildGridState` properties to use `Grid` and `ApiResponse` domain types, eliminating primitive obsession. Extracted and completed `buildChecksumPayload` as a unified helper. Eliminated all `eslint-disable perfectionist/sort-objects` inline comments by rewriting payload construction with sequential property assignment (`payload[KEY] = value` style), which preserves critical insertion order without triggering the lint rule. Fixed `initialGridDefinition: undefined` serialization via `?? null` to ensure the key always appears in JSON output for checksum consistency. Updated the test suite with new `buildChecksumPayload` key-ordering, insertion-order stability, and checksum payload verification tests.
Key Learnings:
- Computed property keys in object literals (e.g., `{ [KEY]: value }`) are still checked by `perfectionist/sort-objects` against the string values of those keys. The rule sees the computed value, not just the presence of brackets. Sequential property assignment on an empty object (`const o = {}; o[KEY] = value`) is the correct approach to bypass this rule without `eslint-disable` comments.
- `JSON.stringify` drops properties with `undefined` values. When checksum stability requires a key to always be present, normalize `undefined` to `null` using `?? null` before serialization.

Date: 2026-08-29
Task: Implement GitHub issue #722: "refactor(tech): implement restoreTechState store action"
Summary: Confirmed that the `restoreTechState` store action, JSDoc signature, and comprehensive test suite are fully implemented, type-safe, and validated. Verified that checked modules, solver parameters, and legacy inputs are correctly restored and fallbacks are handled without direct state mutation in hooks. All tests and linter checks pass cleanly.
Key Learnings:
- Restoring tech state using a dedicated action on the store prevents direct state mutation in React hooks, maintaining clean store encapsulation boundaries.
- Re-verifying previously completed issue requirements through the test suite and TypeScript compilers ensures that the implementation remains robust and does not regress.

Date: 2026-08-29
Task: Fix default checked modules removal on grid reset
Summary: Removed the redundant and incorrect `techStore.clearAllModuleSelections()` call from `sessionCoordinator.resetSession()`. In the consolidated store, both `clearAllCheckedModules` and `clearAllModuleSelections` modify the same `checkedModules` state registry. `clearAllCheckedModules` correctly restores the default checked modules from the tech tree, but `clearAllModuleSelections` immediately cleared it to `{}`, causing the optimization button to become disabled. Kept `clearAllModuleSelections` in `switchPlatform` since platform changes should reset all custom selections. Added `resetGrid` and `clearAllCheckedModules` mocks to `sessionCoordinator.test.ts` and added a unit test to verify `resetSession` behavior.
Key Learnings:
- When consolidating multiple independent stores into a single Zustand store, we must review the sequence of store actions called in transaction workflows (like resetting or switching state). Actions that previously modified separate states might now write to the same fields, causing unintended overrides.
- Resetting a session should restore default/baseline configurations (like standard ship core modules and their selections) rather than completely wiping all data structures. Complete wipes should be reserved for global lifecycle transitions like switching ship platforms.

Date: 2026-08-29
Task: Migrate Hook useGridCellInteraction to Thin DOM Adapter (GitHub #728)
Summary: Migrated `useGridCellInteraction` from a fat hook with validation, metrics, and shake triggers to a thin DOM adapter routing mouse, keyboard, accessibility, and touch events directly to store actions. Shifted layout invariant validators and gesture transaction timing (single vs double tap detection) directly into the store module (`registerCellTap`). Added layout lock and slot validations inside store mutator actions (`toggleCellActive`, `toggleCellSupercharged`, `setCellActive`, and `setCellSupercharged`) as well. Replaced the hook's unit test suite with simplified event-routing verification. Created `registerCellTap.test.ts` to fully cover store-level gestures, rollback transactions, and UI telemetry.
Key Learnings:
- Thinning out React hooks into pure DOM adapters enforces strict module boundaries and guarantees store state consistency, regardless of where or how mutations are triggered.
- Moving touch-timing and validation constraints into store-level transactions allows for direct unit testing of invariants without simulating fragile browser events.

Date: 2026-08-29
Task: Implement GitHub issue #729: "fix(session): reset validation violation counters on grid reset"
Summary: Integrated `useUiStore` inside the `sessionCoordinator`'s `resetSession` method, calling `useUiStore.getState().resetSession()` to clear all session violation counters (such as grid fixed count, module locked count, row limit count, supercharged fixed count, and supercharged limit count) and the shake timer. Added corresponding mocks and unit test assertions in `src/store/sessionCoordinator.test.ts` to verify the integration.
Key Learnings:
- Global state coordinators should orchestrate clear-downs across all consolidated stores (domain stores like Grid/Tech and UI presentation stores) during global operations like a session reset.
- Mocking consolidated stores selectively in orchestrator test suites using `vi.mock` prevents side-effects and isolates test assertions.

Date: 2026-08-29
Task: Implement GitHub issue #727: "Implement Tap Transaction Action registerCellTap in useGridStore"
Summary: Verified the existing implementation of the touch sequence transaction action `registerCellTap` in `useGridStore` and its test suite `registerCellTap.test.ts` (consisting of 9 tests). Documented the action in `gridTypes.ts` with JSDoc conforming to the `agentic-jsdoc` style. Verified all tests and linters pass cleanly.
Key Learnings:
- Standardizing store actions with clean TypeDoc/JSDoc structures conforming to `agentic-jsdoc` improves developer/LLM comprehension.
- Cross-verifying and closing related sub-issues helps maintain issue tracker health and ensures all validation rules and rollback paths are properly tested.

Date: 2026-08-29
Task: Implement GitHub issue #726: "Consolidate Grid Layout Validations inside useGridStore"
Summary: Refactored `toggleCellActive`, `toggleCellSupercharged`, `setCellActive`, and `setCellSupercharged` actions in `useGridStore` to enforce layout-lock, module-lock, row-limit, and supercharged-limit validations, trigger the UI shake animation, increment uiStore violation counters, and return a boolean success status. Added the test suite `actionsValidation.test.ts` to verify the validations and return values.
Key Learnings:
- Standardizing store actions to run their own invariants and return a transaction status simplifies client hooks and guarantees state safety.
- Spying on global store methods in Vitest allows for reliable assertions on global state telemetry and side-effects.

Date: 2026-08-29
Task: Implement GitHub issue #733: "fix(test): rename baseMockMockGridStoreState typo in useGridCellInteraction test"
Summary: Renamed the factory function `baseMockMockGridStoreState` typo to `createMockGridStoreState` at its definition and call site in `src/components/GridCell/useGridCellInteraction.test.ts`. Verified the test suite runs and passes cleanly under Vitest.
Key Learnings:
- Renaming utility helpers to explicitly declare their factory intent (e.g. `createMockGridStoreState`) clarifies test code readability.
- Chained test run tasks in npm/bun scripts can forward file arguments to all sub-commands; when running tests on single Vitest files, calling `vitest run` directly is cleaner than chaining through scripts that forward parameters to Bun's native runner.

2026-08-29: fix(grid): remove eslint-disable by fixing JSDoc references in gridTypes.ts (#732)
2026-08-29: refactor(grid): extract applyValidationFeedback helper and refactor store actions (#731)
2026-08-29: refactor(tech): refactor useTechModuleManagement and Dialogs to delegate to deep techStore (#737)
2026-08-29: feat(tech): integrate technology validation rules engine into techStore (#736)
