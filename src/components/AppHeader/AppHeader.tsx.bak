// src/components/AppHeader/AppHeader.tsx
import "./AppHeader.css";

import type { FC } from "react";
import { useTranslation } from "react-i18next";

import { useDialog } from "../../context/dialog-utils";
import LanguageSelector from "../LanguageSelector/LanguageSelector";
import { GitHubIcon, NmsSoIcon, ShareIcon } from "../Icons/Icons";

interface AppHeaderProps {
	buildVersion: string;
}

const AppHeader: FC<AppHeaderProps> = ({ buildVersion }) => {
	const { t } = useTranslation();
	const { openDialog } = useDialog();

	return (
		<header className="app-header">
			<div className="header-left">
				<a href="/" className="logo-link">
					<NmsSoIcon />
					<h1 className="app-title">
						<span className="title-normal">NMS</span>
						<span className="title-highlight">Optimizer</span>
					</h1>
				</a>
				<p className="app-version">v{buildVersion}</p>
			</div>
			<div className="header-right">
				<nav className="main-nav">
					<a href="#" onClick={() => openDialog("share")}>
						<ShareIcon />
						{t("appHeader.buttons.share")}
					</a>
					<a href="https://github.com/jbelew/nms-optimizer-web" target="_blank" rel="noreferrer">
						<GitHubIcon />
						{t("appHeader.buttons.github")}
					</a>
				</nav>
				<div className="language-selector-container">
					<LanguageSelector />
				</div>
			</div>
		</header>
	);
};

export default AppHeader;
