# No catch-all redirect. Let real 404s happen for missing SSG files.
# SPA fallbacks for client-only routes are handled in functions/[[path]].js
# because Pages Functions take precedence over _redirects.
