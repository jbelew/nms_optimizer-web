# Exclude non-code JSON data, configuration, and metadata files
*.json
**/*.json

# Exclude agent metadata and conductor task files
.agents/
conductor/

# Exclude graphify backups
graphify-out/*/

# Exclude localization assets (markdown and text content, not code)
public/assets/locales/
